apiVersion: v1
kind: Service
metadata:
  name: order-management-api-service
spec:
  selector:
    app: order-management-api
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000